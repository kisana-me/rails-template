<% content_for :title, "文書一覧" %>

<h1>文書一覧</h1>

<div class="documents">
  <% @documents.each do |document| %>
    <div>
      <%= document.title %>
    <br />
      <%= link_to "詳細へ", document_path(document.name_id) %>
    </div>
  <% end %>
</div>
